{
  "name": "sogo-canvas-workspace",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "extension",
    "webview"
  ],
  "scripts": {
    "build": "npm -w webview run build && npm -w extension run build",
    "watch": "npm -w webview run dev & npm -w extension run watch",
    "typecheck": "npm -w webview run typecheck && npm -w extension run typecheck"
  }
}

